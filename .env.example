# Copy to .env and fill in.
# .env is git-ignored.

# JWT signing key. 32+ random bytes.
#   openssl rand -hex 32
JWT_SECRET=

# Dashboard admin credentials.
ADMIN_USER=admin
#   openssl rand -hex 16
ADMIN_PASSWORD=
